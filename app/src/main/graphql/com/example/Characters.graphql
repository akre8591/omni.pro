query Characters($page: Int) {
    characters(page: $page) {
        results {
            id
            name
            status
            species
            origin {
                name
            }
            image
            location {
                name
            }
        }
    }
}